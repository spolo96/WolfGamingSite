import { IStoreKey } from './storeKey';
import { ISubscribable } from './ISubscribable';
export declare class StoreSet {
    private _stores;
    constructor();
    add<T extends ISubscribable>(key: IStoreKey, value: T): StoreSet;
    getStore<T extends ISubscribable>(key: IStoreKey): T;
    merge(stores: StoreSet): StoreSet;
}
